#!/bin/bash


# "Concatenating" all selected questions/answers/notes into a single TeX file.
# This is where you specify the questions you want included in your worksheet, in the
# order you want them to appear.
rm -vf all.tex
touch all.tex
cat i02271no.tex >> all.tex # PLC, I/O: analog resolution and scaling
cat i02546no.tex >> all.tex # PLC, I/O: analog resolution and scaling
cat n00002.tex >> all.tex
cat n00003.tex >> all.tex
cat n00004.tex >> all.tex
