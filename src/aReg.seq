#!/bin/bash
rm -vf all.tex
touch all.tex


cat i04808.tex >> all.tex # definisjoner
cat i04810.tex >> all.tex # tegne blokkskjema
cat i01469.tex >> all.tex # graf av PID
cat i01368.tex >> all.tex # feilsøking  
cat i00788.tex >> all.tex # direkte eller reverserende
cat i01656.tex >> all.tex # finn prosess data
cat i01530.tex >> all.tex # feilsøking 
cat i01776.tex >> all.tex # implementer foroverkobling
cat i04809.tex >> all.tex # optimalisering av prosess
