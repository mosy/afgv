#!/bin/bash
rm -vf all.tex
touch all.tex


cat i04808.tex >> all.tex # definisjoner
cat i04810.tex >> all.tex # tegne blokkskjema
cat i00788p.tex >> all.tex # direkte eller reverserende
cat i01468p.tex >> all.tex # graf av PID
cat i01368p.tex >> all.tex # feilsøking  
cat i01656.tex >> all.tex # finn prosess data
cat i01530p.tex >> all.tex # feilsøking 
cat i00424.tex >> all.tex # implementer foroverkobling
cat i04809.tex >> all.tex # optimalisering av prosess
